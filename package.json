{
  "name": "clockbot",
  "version": "1.0.0",
  "description": "Discord clock in/out bot (buttons + slash), temp storage (/tmp), Render Web Service with HTTP keepalive",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "discord.js": "^14.15.3"
  }
}
